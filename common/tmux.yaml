session_name: "meuh"
windows:
- window_name: dev window
  layout: tiled
  panes:
    - shell_command:
      - cd api
      - rails s -b 0.0.0.0
    - shell_command:
      - cd frontend
      - npm run client:dev        
